<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{path('home_page')}}">SymBNB !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{path('home_page')}}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('ads_liste')}}">Annonces</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link btn btn-primary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <img src="{{app.user.picture}}" alt="" class="avatar-min">
                        <span>{{ app.user.fullName }}</span>    
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                        <a class="dropdown-item" href="{{path("ad_create")}}">Créer une annonce</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{path("account_user")}}">Mon Compte</a>
                        <a class="dropdown-item" href="{{path("account_bookings")}}">Mes Réservations</a>
                        <a class="dropdown-item" href="{{path("account_edit")}}">Modifier mon profile </a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{path("account_logout")}}">Déconnexion</a>
                        </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('account_login')}}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('account_registration')}}">Inscription</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>